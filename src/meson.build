# add_project_arguments('-DCLOG_CUSTOM_CONFIG', language: 'c')

libclog_sources = [
	'clog.c',
]

libclog = static_library(
	'clog',
	libclog_sources,
	include_directories: [ rootInclude, ],
	c_args: [],
)
